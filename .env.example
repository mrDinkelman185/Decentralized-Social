# Server Configuration
PORT=3333
SESSION_SECRET=generate-with-node-crypto-randomBytes-64
NODE_ENV=development

# Database
MONGODB_URI=mongodb://127.0.0.1/nodeStream

# RTMP Server
RTMP_PORT=1935
RTMP_HTTP_PORT=8888
FFMPEG_PATH=/usr/bin/ffmpeg
CORS_ORIGIN=http://localhost:3000

# API Keys (use your own; rotate if ever exposed)
ALCHEMY_KEY=your-alchemy-key
INFURA_KEY=your-infura-key
SENDGRID_API_KEY=your-sendgrid-key
SENDGRID_MAIL=your-sendgrid-email

# Pinata (IPFS) - prefer server-side usage; add to .env if using from frontend
PINITA_API_KEY=your-pinata-key
PINITA_API_SECRET=your-pinata-secret

# Network (chain ID, e.g. 11155111 for Sepolia)
NETWORK_ID=11155111

# Cookie expiration (days)
COOKIE_EXPIRE=7
COOKIE_EXPIRES_TIME=7

# Optional: Moralis API (only if using NFT/price features; add MORALIS_API_KEY if needed)

# Optional: Developer info (base64-encoded, for WalletConnectorModal)
DEVELOPER_URL_BTOA=base64-encoded-url-here
DEVELOPER_NAME_BTOA=base64-encoded-name-here
